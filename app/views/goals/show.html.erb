<p id="notice"><%= notice %></p>

<div>
  <h3><%= @goal.user.name %></h3>
  <h3><%= @goal.title %></h3>
  <h3><%= @goal.detail %></h3>
</div>

<div>
  <% if @goal.user_id == current_user.id %>
    <%= link_to '編集', edit_goal_path(@goal)%>
  <% end %>
  <%= link_to '戻る', goal_path%>
</div>
