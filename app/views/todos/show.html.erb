<p id="notice"><%= notice %></p>

<div>
  <h3><%= @todo.body %></h3>
  <h3><%= @todo.link %></h3>
  <h3><%= @todo.status %></h3>
</div>

<div>
  <% if @todo.user_id == current_user.id %>
    <%= link_to '編集', edit_todo_path(@todo)%>
  <% end %>
  <%= link_to '戻る', todo_path%>
</div>
