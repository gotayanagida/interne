<p id="notice"><%= notice %></p>

<div>
  <h3><%= @task.user.name %></h3>
  <h3><%= @task.title %></h3>
  <h3><%= simple_format(@task.detail) %></h3>
</div>

<div>
  <% if @task.user_id == current_user.id %>
    <%= link_to '編集', edit_task_path(@task)%>
  <% end %>
  <%= link_to '戻る', task_path%>
</div>
