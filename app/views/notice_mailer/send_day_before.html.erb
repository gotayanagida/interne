<!doctype html>
<html lang="ja">
<head>
  <meta content="text/html; charset=UTF-8" />
</head>
<body>

<%= @staff.name %>さん

日頃のお仕事お疲れ様です！

明日は以下のインターン生が出社します。
<% @schedules.each do |schedule| %>
  <%= schedule.user.name %>さん：
  <%= schedule.work_started_at.strftime("%-H時%m分") %> ~
  <%= schedule.work_ended_at.strftime("%-H時%m分") %><br>
<% end %>

インテルネ運営事務局

</body>
</html>
